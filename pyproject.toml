[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "knowledge-index"
version = "0.1.0"
description = "Personal knowledge indexer"
requires-python = ">=3.12"
dependencies = []